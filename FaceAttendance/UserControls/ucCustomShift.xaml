<UserControl
    x:Class="FaceAttendance.UserControls.ucCustomShift"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FaceAttendance.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="850"
    d:DesignWidth="1024"
    Loaded="UserControl_Loaded"
    mc:Ignorable="d">
    <!--  Height="428"  -->
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />

    </UserControl.Resources>
    <Grid Name="MainGrid" Height="auto">
        <StackPanel Margin="0,0,0,0" DockPanel.Dock="Top">
            <StackPanel>
                <Label
                    Margin="0,0,0,0"
                    Content="Deptartment Management"
                    FontSize="20"
                    Foreground="#2b7dbc" />
                <StackPanel
                    Margin="0,20,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Orientation="Horizontal">
                    <Grid
                        Width="50"
                        Height="30"
                        Margin="0,0,10,0"
                        Cursor="Hand"
                        Focusable="True"
                        MouseDown="AddDev_MouseDown"
                        ToolTip="Add New Department">
                        <Border
                            BorderBrush="{StaticResource VISION}"
                            BorderThickness="1,0,0,1"
                            CornerRadius="5">
                            <Border
                                Height="60"
                                BorderBrush="{StaticResource HIK}"
                                BorderThickness="0,1,1,0"
                                CornerRadius="10" />
                            <Border.Background>
                                <LinearGradientBrush MappingMode="RelativeToBoundingBox" StartPoint="0.5,0" EndPoint="0.5,1">
                                    <GradientStop Color="{StaticResource RoundBack}" />
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect Color="{StaticResource VISIONC}" />
                            </Border.Effect>
                        </Border>
                        <Image
                            Name="AddDev"
                            Width="50"
                            Height="30"
                            Margin="3,0,3,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom">
                            <Image.Source>
                                <BitmapImage UriSource="..\Images\add.png" />
                            </Image.Source>
                        </Image>
                        <!--<materialDesign:PackIcon
                            Margin="3,0,3,0"
                                Height="30"
                                Width="50"
                                Name="AddDev"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom"
                            Foreground="{StaticResource Success}"
                            Kind="AddCircleOutline"   />-->
                    </Grid>
                    <Grid
                        Name="ModDev"
                        Width="50"
                        Height="30"
                        Cursor="Hand"
                        Focusable="True"
                        MouseDown="Border_MouseDown"
                        ToolTip="Edit Department">
                        <Border
                            BorderBrush="{StaticResource VISION}"
                            BorderThickness="1,0,0,1"
                            CornerRadius="10">
                            <Border
                                Height="60"
                                BorderBrush="{StaticResource HIK}"
                                BorderThickness="0,1,1,0"
                                CornerRadius="5" />
                            <Border.Background>
                                <LinearGradientBrush MappingMode="RelativeToBoundingBox" StartPoint="0.5,0" EndPoint="0.5,1">
                                    <GradientStop Color="{StaticResource RoundBack}" />
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect Color="{StaticResource HIKC}" />
                            </Border.Effect>
                        </Border>
                        <Image
                            Name="Modify"
                            Width="50"
                            Height="30"
                            Margin="3,0,3,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom">
                            <Image.Source>
                                <BitmapImage UriSource="..\Images\mode_edit.png" />
                            </Image.Source>
                        </Image>
                        <!--<materialDesign:PackIcon
                            Name="Modify"
                                Height="30"
                                Width="50"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Top"
                            Foreground="{StaticResource Alert}"
                            Kind="Edit" />-->

                        <!--<Image
                            Name="minic"
                            Width="50"
                            Height="30"
                            Margin="3,0,3,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom">
                            <Image.Source>
                                <BitmapImage UriSource="U:\FaceAttendance Versions\FaceAttendance ver 1.18''\FaceAttendance\Images\fullscreen_exit.png" />
                            </Image.Source>
                        </Image>-->
                        <!--<materialDesign:PackIcon
                            Name="minic"
                            Margin="3,0,3,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom"
                            Foreground="{StaticResource Minimize}"
                            Kind="FullscreenExit"
                            Visibility="Collapsed" />-->
                    </Grid>
                    <Grid
                        Width="50"
                        Height="30"
                        Margin="10,0,0,0"
                        Cursor="Hand"
                        Focusable="True"
                        MouseDown="Grid_MouseDown"
                        ToolTip="Delete Department">
                        <Border
                            BorderBrush="{StaticResource VISION}"
                            BorderThickness="1,0,0,1"
                            CornerRadius="5">
                            <Border
                                Height="60"
                                BorderBrush="{StaticResource HIK}"
                                BorderThickness="0,1,1,0"
                                CornerRadius="10" />
                            <Border.Background>
                                <LinearGradientBrush MappingMode="RelativeToBoundingBox" StartPoint="0.5,0" EndPoint="0.5,1">
                                    <GradientStop Color="{StaticResource RoundBack}" />
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect Color="{StaticResource CloseC}" />
                            </Border.Effect>
                        </Border>
                        <Image
                            Name="Delete"
                            Width="50"
                            Height="30"
                            Margin="3,0,3,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom">
                            <Image.Source>
                                <BitmapImage UriSource="..\Images\Delete.png" />
                            </Image.Source>
                        </Image>
                        <!--<materialDesign:PackIcon
                           Margin="3,0,3,0"
                                Height="30"
                                Width="50"
                                Name="Delete"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom"
                            Foreground="{StaticResource Close}"
                            Kind="Delete" />-->
                    </Grid>

                </StackPanel>
                <TextBlock Margin="0,15,10,5" HorizontalAlignment="Left">
                    <Label
                        Width="120"
                        Content="MIN TIME IN"
                        FontSize="13"
                        Foreground="Black" />
                    <Grid
                        Width="370"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Background="White">
                        <!--  overlay with hint text  -->

                        <TextBlock
                            Name="txtName"
                            Width="370"
                            Height="30"
                            Padding="5,10"
                            VerticalAlignment="Center"
                            FontSize="11"
                            Foreground="#777A7D"
                            Text="Enter MIN TIME IN hh:mm"
                            TextAlignment="Left" />

                        <!--  enter term here  -->
                        <TextBox
                            Name="txtTimeIn"
                            Width="370"
                            Height="30"
                            Padding="5,5"
                            Background="Transparent"
                            GotFocus="txtDeptDesc_GotFocus"
                            IsReadOnly="True" />

                    </Grid>
                    <!--<InlineUIContainer>
                        <TextBox
                            Name="txtIP"
                            Width="370"
                            Height="30"
                            VerticalContentAlignment="Center"
                            Foreground="Black"
                            IsReadOnly="True">
                            <TextBox.ToolTip>
                                <ItemsControl>
                                    <Label FontSize="14">Dev Ip</Label>
                                </ItemsControl>
                            </TextBox.ToolTip>
                        </TextBox>
                    </InlineUIContainer>-->

                    <Label
                        Width="120"
                        Margin="20,0,0,0"
                        Content="MAX TIME OUT"
                        FontSize="13"
                        Foreground="Black"
                        Visibility="Visible" />
                    <Grid
                        Width="370"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Background="White"
                        Visibility="Visible">
                        <!--  overlay with hint text  -->

                        <TextBlock
                            Name="txtDes"
                            Width="370"
                            Height="30"
                            Padding="5,10"
                            VerticalAlignment="Center"
                            FontSize="11"
                            Foreground="#777A7D"
                            Text="Enter Department Description"
                            TextAlignment="Left" />

                        <!--  enter term here  -->
                        <TextBox
                            Name="txtTimeOut"
                            Width="370"
                            Height="30"
                            Padding="5,5"
                            Background="Transparent"
                            GotFocus="txtDevName_GotFocus"
                            IsReadOnly="True" />

                    </Grid>
                    <!--<TextBox
                        Name="txtDevID"
                        Width="370"
                        Height="30"
                        VerticalContentAlignment="Center"
                        Foreground="Black"
                        IsReadOnly="True"
                        MaxLength="3"
                        Text="e.g 1,2,3"
                        TextChanged="getSerialOnChange"
                        TextInput="empty_dev_id" />-->

                </TextBlock>



                <StackPanel DockPanel.Dock="Top">


                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="256*" />
                            <ColumnDefinition Width="89*" />
                            <ColumnDefinition Width="423*" />
                            <ColumnDefinition Width="256*" />
                        </Grid.ColumnDefinitions>
                        <Button
                            Name="btnsave"
                            Grid.Column="3"
                            Margin="0,0,15,0"
                            Padding="20,9"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Click="btnsave_Click"
                            Content="Save"
                            Focusable="True"
                            FontSize="14"
                            IsEnabled="False"
                            Style="{StaticResource 3DButtonStyle}"
                            TabIndex="6" />

                    </Grid>

                </StackPanel>
                <Label
                    Margin="0,10,0,0"
                    Content="Device Information"
                    FontSize="20"
                    Foreground="#2b7dbc" />


                <StackPanel
                    Height="auto"
                    Margin="0,5,0,0"
                    HorizontalAlignment="Stretch">
                    <!--  This is the hidden helper Grid which does the resizing  -->
                    <Grid Visibility="Hidden">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>
                        <!--  This is the hidden helper Field which is used to bind to, using the "Fill" column of the helper grid  -->
                        <Grid x:Name="c1" Grid.Column="0" />
                        <Grid x:Name="c2" Grid.Column="1" />
                        <Grid x:Name="c3" Grid.Column="2" />
                        <Grid x:Name="c4" Grid.Column="3" />
                        <Grid x:Name="c5" Grid.Column="4" />
                        <Grid x:Name="c6" Grid.Column="5" />
                    </Grid>
                    <ListView
                        x:Name="lvdevinfo"
                        Height="500"
                        Margin="0,0,4,0"
                        VerticalContentAlignment="Top"
                        Background="Transparent"
                        FontSize="22"
                        Foreground="Black"
                        ItemsSource="{Binding Devices}"
                        ScrollViewer.CanContentScroll="True"
                        ScrollViewer.HorizontalScrollBarVisibility="Visible"
                        ScrollViewer.VerticalScrollBarVisibility="Visible">
                        <ListView.Resources>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Height" Value="30" />

                            </Style>
                        </ListView.Resources>
                        <ListView.View>

                            <GridView>
                                <GridView.ColumnHeaderContainerStyle>
                                    <Style TargetType="{x:Type GridViewColumnHeader}">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                                                    <Border
                                                        Background="Transparent"
                                                        BorderBrush="Black"
                                                        BorderThickness="0,0,0,0">
                                                        <TextBlock
                                                            x:Name="ContentHeader"
                                                            Width="{TemplateBinding Width}"
                                                            Padding="15,5,5,5"
                                                            Text="{TemplateBinding Content}"
                                                            TextAlignment="Left" />
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="OverridesDefaultStyle" Value="True" />
                                        <Setter Property="Foreground" Value="{StaticResource FREE}" />
                                        <Setter Property="FontFamily" Value="Champagne &amp; Limousines" />
                                        <Setter Property="FontSize" Value="12" />
                                        <Setter Property="FontWeight" Value="Bold" />
                                        <Setter Property="Background" Value="AliceBlue" />
                                        <Setter Property="BorderBrush" Value="BlanchedAlmond" />
                                        <Setter Property="BorderThickness" Value="2" />

                                    </Style>
                                </GridView.ColumnHeaderContainerStyle>
                                <GridViewColumn Width="203" Header="ID">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock
                                                Padding="0"
                                                HorizontalAlignment="Stretch"
                                                VerticalAlignment="Stretch">
                                                <Label
                                                    Padding="0"
                                                    Content="{Binding id}"
                                                    FontSize="12" />
                                            </TextBlock>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="{Binding ElementName=c2, Path=ActualWidth}" Header="TIME IN">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock
                                                Padding="0"
                                                HorizontalAlignment="Stretch"
                                                VerticalAlignment="Stretch">
                                                <Label
                                                    Padding="0"
                                                    Content="{Binding Time_IN}"
                                                    FontSize="12" />
                                            </TextBlock>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="205" Header="TIME OUT">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock
                                                Padding="0"
                                                HorizontalAlignment="Stretch"
                                                VerticalAlignment="Stretch">
                                                <Label
                                                    Padding="0"
                                                    Content="{Binding Time_OUT}"
                                                    FontSize="12" />
                                            </TextBlock>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="0" Header="H.W Pin">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>

                                            <TextBlock
                                                Padding="0"
                                                HorizontalAlignment="Stretch"
                                                VerticalAlignment="Stretch">
                                                <Label
                                                    Padding="0"
                                                    Content="{Binding HWpin}"
                                                    FontSize="12" />
                                            </TextBlock>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="0" Header="Last Active Status">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>

                                            <TextBlock
                                                Padding="0"
                                                HorizontalAlignment="Stretch"
                                                VerticalAlignment="Stretch">
                                                <Label
                                                    Padding="0"
                                                    Content="{Binding Status}"
                                                    FontSize="12" />
                                            </TextBlock>
                                        </DataTemplate>

                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>


                                <GridViewColumn
                                    x:Name="action"
                                    Width="0"
                                    Header="Action">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock
                                                Padding="0"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Stretch">
                                                <TextBlock
                                                    Name="abc"
                                                    Width="65"
                                                    Cursor="Hand"
                                                    Focusable="True">
                                                    <CheckBox IsChecked="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}, Path=IsSelected}" />
                                                    <TextBlock Margin="0,2,2,0" HorizontalAlignment="Center">
                                                        <Label
                                                            Padding="0,0,0,3"
                                                            FontFamily="Champagne &amp; Limousines"
                                                            FontSize="11"
                                                            Foreground="{StaticResource VISION}" />
                                                    </TextBlock>
                                                </TextBlock>

                                            </TextBlock>

                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>


                            </GridView>
                        </ListView.View>

                    </ListView>

                </StackPanel>
                <!--<Grid Height="390" VerticalAlignment="Top">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50" />
                        <RowDefinition Height="2*" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Height="50">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.5*" />
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="1.5*" />

                        </Grid.ColumnDefinitions>

                        <Grid
                            Grid.Column="0"
                            Width="200"
                            Height="30"
                            Margin="0,0,0,0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Background="White">
                -->
                <!--  overlay with hint text  -->
                <!--

                            <TextBlock
                                Name="txt_search1"
                                Width="200"
                                Height="30"
                                Padding="5,10"
                                VerticalAlignment="Center"
                                FontSize="11"
                                Foreground="#777A7D"
                                Text="Enter User Id"
                                TextAlignment="Left" />

                -->
                <!--  enter term here  -->
                <!--
                            <TextBox
                                Name="txt_search"
                                Width="200"
                                Height="30"
                                Padding="5,5"
                                Background="Transparent"
                                TextChanged="search_text_changed" />

                        </Grid>

                -->
                <!--<TextBox
                                x:Name="txt_search1"
                                Grid.Column="0"
                                Width="auto"
                                Height="30"
                                Margin="0,0,0,0"
                                FontSize="12"
                                Foreground="Black"
                                Text=""
                                TextChanged="Txt_chnged_ItemSo_reset" />-->
                <!--
                        <Button
                            Grid.Column="0"
                            Padding="25,9"
                            HorizontalAlignment="Right"
                            Content="Search"
                            Style="{StaticResource 3DButtonStyle}" />
                        <Image
                            Name="U_Wait"
                            Grid.Column="1"
                            Width="50"
                            Height="30"
                            Margin="0,0,150,0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                            <Image.Source>
                                <BitmapImage UriSource="..\Images\waitsand.png" />
                            </Image.Source>
                        </Image>
                        <Button
                            Grid.Column="2"
                            Padding="25,9"
                            HorizontalAlignment="Left"
                            Content="Assign Shift"
                            Style="{StaticResource 3DButtonStyle}" />

                        <Grid
                            Grid.Column="2"
                            Width="30"
                            Height="30"
                            MouseDown="refreshClick">
                            <Image
                                Name="AddUser"
                                Grid.Column="2"
                                Width="30"
                                Height="30"
                                Margin="0,0,0,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center">
                                <Image.Source>
                                    <BitmapImage UriSource="..\Images\refresh.png" />
                                </Image.Source>
                            </Image>
                        </Grid>


                    </Grid>

                    <Grid Grid.Row="1" Height="auto">



                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="6*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="6*" />
                        </Grid.ColumnDefinitions>


                        <StackPanel Grid.Column="0">
                            <Border
                                Height="auto"
                                BorderBrush="{StaticResource VISION}"
                                BorderThickness="1"
                                CornerRadius="5"
                                RenderTransformOrigin="0.5,0.5">


                                <StackPanel>
                                    <Grid Visibility="Hidden">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1*" />
                                            <ColumnDefinition Width="1*" />
                                            <ColumnDefinition Width="1*" />
                                            <ColumnDefinition Width="1*" />
                                            <ColumnDefinition Width="1*" />

                                        </Grid.ColumnDefinitions>
                -->
                <!--  This is the hidden helper Field which is used to bind to, using the "Fill" column of the helper grid  -->
                <!--
                                        <Grid x:Name="dbc0" Grid.Column="0" />
                                        <Grid x:Name="dbc1" Grid.Column="1" />
                                        <Grid x:Name="dbc2" Grid.Column="2" />
                                        <Grid x:Name="dbc3" Grid.Column="3" />
                                        <Grid x:Name="dbc4" Grid.Column="4" />

                                    </Grid>
                                    <ListView
                                        x:Name="dbuserlist"
                                        Height="400"
                                        MaxHeight="336"
                                        Margin="0,0,4,0"
                                        VerticalAlignment="Top"
                                        VerticalContentAlignment="Top"
                                        Background="#fafafa"
                                        FontSize="12"
                                        Foreground="Black"
                                        ItemsSource="{Binding Udown_Disp1}"
                                        ScrollViewer.CanContentScroll="True"
                                        ScrollViewer.HorizontalScrollBarVisibility="Visible"
                                        ScrollViewer.VerticalScrollBarVisibility="Visible"
                                        SelectionMode="Multiple">
                                        <ListView.Resources>
                                            <Style TargetType="{x:Type ListViewItem}">
                                                <Setter Property="Height" Value="0" />

                                            </Style>
                                        </ListView.Resources>

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="{x:Type ListViewItem}">

                                                <Setter Property="IsSelected" Value="{Binding SomeItemSelected, Mode=TwoWay}" />

                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource HIK}" />
                                                        <Setter Property="Focusable" Value="True" />


                                                    </Trigger>

                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource HIK}" />

                                                    </Trigger>

                                                </Style.Triggers>
                                            </Style>


                                        </ListView.ItemContainerStyle>

                                        <ListView.View>

                                            <GridView>
                                                <GridView.ColumnHeaderContainerStyle>
                                                    <Style TargetType="{x:Type GridViewColumnHeader}">
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                                                                    <Border
                                                                        Background="Transparent"
                                                                        BorderBrush="Black"
                                                                        BorderThickness="0,0,0,0">
                                                                        <TextBlock
                                                                            x:Name="ContentHeader"
                                                                            Width="{TemplateBinding Width}"
                                                                            Padding="15,5,5,5"
                                                                            Text="{TemplateBinding Content}"
                                                                            TextAlignment="Left" />
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Setter Property="OverridesDefaultStyle" Value="True" />
                                                        <Setter Property="Foreground" Value="{StaticResource FREE}" />
                                                        <Setter Property="FontFamily" Value="Champagne &amp; Limousines" />
                                                        <Setter Property="FontSize" Value="12" />
                                                        <Setter Property="FontWeight" Value="Bold" />
                                                        <Setter Property="Background" Value="AliceBlue" />
                                                        <Setter Property="BorderBrush" Value="BlanchedAlmond" />
                                                        <Setter Property="BorderThickness" Value="2" />

                                                    </Style>
                                                </GridView.ColumnHeaderContainerStyle>



                                                <GridViewColumn Width="{Binding ActualWidth, ElementName=dbc0}" Header="Dev ID">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                                Padding="0"
                                                                HorizontalAlignment="Stretch"
                                                                VerticalAlignment="Stretch"><InlineUIContainer>
                                                                    <Label
                                                                        Padding="0"
                                                                        Content="{Binding EMachineNumber}"
                                                                        FontSize="12" />
                                                                </InlineUIContainer></TextBlock>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>

                                                <GridViewColumn Width="{Binding ActualWidth, ElementName=dbc1}" Header="User Id">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                                Padding="0"
                                                                HorizontalAlignment="Stretch"
                                                                VerticalAlignment="Stretch"><InlineUIContainer>
                                                                    <Label
                                                                        Padding="0"
                                                                        Content="{Binding EnrollNumber}"
                                                                        FontSize="12" />
                                                                </InlineUIContainer></TextBlock>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>

                                                <GridViewColumn Width="{Binding ActualWidth, ElementName=dbc2}" Header="Name">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                                Padding="0"
                                                                HorizontalAlignment="Stretch"
                                                                VerticalAlignment="Stretch"><InlineUIContainer>
                                                                    <Label
                                                                        Padding="0"
                                                                        Content="{Binding EName}"
                                                                        FontSize="12" />
                                                                </InlineUIContainer></TextBlock>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>

                                                <GridViewColumn Width="{Binding ActualWidth, ElementName=dbc3}" Header="Password">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                                Padding="0"
                                                                HorizontalAlignment="Stretch"
                                                                VerticalAlignment="Stretch"><InlineUIContainer>
                                                                    <Label
                                                                        Padding="0"
                                                                        Content="{Binding enPassword}"
                                                                        FontSize="12" />
                                                                </InlineUIContainer></TextBlock>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>


                                                <GridViewColumn
                                                    x:Name="dbshift_C"
                                                    Width="{Binding ActualWidth, ElementName=dbc4}"
                                                    Header="Dpt ID">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                                Padding="0"
                                                                HorizontalAlignment="Stretch"
                                                                VerticalAlignment="Stretch"><InlineUIContainer>
                                                                    <Label
                                                                        Padding="0"
                                                                        Content="{Binding DptID}"
                                                                        FontSize="12" />
                                                                </InlineUIContainer></TextBlock>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>


                                            </GridView>
                                        </ListView.View>

                                    </ListView>
                                </StackPanel>




                            </Border>

                        </StackPanel>
                        <Grid Grid.Column="1" Height="auto">
                            <Border
                                Height="auto"
                                BorderBrush="{StaticResource VISION}"
                                BorderThickness="2"
                                CornerRadius="1"
                                RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <TransformGroup>
                                        <ScaleTransform />
                                        <SkewTransform AngleX="-0.14" />
                                        <RotateTransform />
                                        <TranslateTransform X="-0.331" />
                                    </TransformGroup>
                                </Border.RenderTransform>
                                <StackPanel
                                    Grid.Column="1"
                                    Height="300"
                                    VerticalAlignment="Bottom"
                                    Orientation="Vertical">



                                    <Image
                                        Name="Arrowb"
                                        Width="50"
                                        Height="30"
                                        Margin="0,30,0,0"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Bottom"
                                        MouseDown="MoveSelectedRight"
                                        RenderTransformOrigin="0.5,0.5">
                                        <Image.Source>
                                            <BitmapImage UriSource="..\Images\reply.png" />
                                        </Image.Source>
                                        <Image.RenderTransform>
                                            <ScaleTransform ScaleX="-1" />
                                        </Image.RenderTransform>
                                    </Image>
                -->
                <!--<materialDesign:PackIcon  RenderTransformOrigin="0.5,0.5"
                                            Margin="12,0"
                                Height="30"
                                Width="50"
                                x:Name="Arrowb"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom"
                            Foreground="{StaticResource HIK}"
                            Kind="Reply" MouseDown="MoveSelectedRight"    >
                                            <materialDesign:PackIcon.RenderTransform>
                                                <ScaleTransform ScaleX="-1" />
                                            </materialDesign:PackIcon.RenderTransform>
                                        </materialDesign:PackIcon>-->
                <!--


                                    <Image
                                        Name="DoubleArrowb"
                                        Width="50"
                                        Height="30"
                                        Margin="0,30,0,0"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Bottom"
                                        MouseDown="MoveAllRight"
                                        RenderTransformOrigin="0.5,0.5">
                                        <Image.Source>
                                            <BitmapImage UriSource="..\Images\reply_all.png" />
                                        </Image.Source>
                                        <Image.RenderTransform>
                                            <ScaleTransform ScaleX="-1" />
                                        </Image.RenderTransform>
                                    </Image>
                -->
                <!--<materialDesign:PackIcon  RenderTransformOrigin="0.5,0.5"
                                            Margin="0,40,0,0"
                                Height="30"
                                Width="50"
                                x:Name="DoubleArrowb"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom"
                            Foreground="{StaticResource HIK}"
                            Kind="ReplyAll" MouseDown="MoveAllRight"    >
                                            <materialDesign:PackIcon.RenderTransform>
                                                <ScaleTransform ScaleX="-1" />
                                            </materialDesign:PackIcon.RenderTransform>
                                        </materialDesign:PackIcon>-->
                <!--



                                    <Image
                                        Name="Arrowf"
                                        Width="50"
                                        Height="30"
                                        Margin="0,30,0,0"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Bottom"
                                        MouseDown="MoveSelectedBack">
                                        <Image.Source>
                                            <BitmapImage UriSource="..\Images\reply.png" />
                                        </Image.Source>
                                    </Image>
                -->
                <!--<materialDesign:PackIcon
                                            Margin="0,30,0,0"
                                Height="30"
                                Width="50"
                                x:Name="Arrowf"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom"
                            Foreground="{StaticResource HIK}"
                            Kind="Reply" MouseDown="MoveSelectedBack"   />-->
                <!--

                                    <Image
                                        Name="doubleArrowf"
                                        Width="50"
                                        Height="30"
                                        Margin="0,30,0,0"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Bottom"
                                        MouseDown="MoveAllBack">
                                        <Image.Source>
                                            <BitmapImage UriSource="..\Images\reply_all.png" />
                                        </Image.Source>
                                    </Image>
                -->
                <!--<materialDesign:PackIcon
                                            Margin="0,30,0,0"
                                Height="30"
                                Width="50"
                                x:Name="doubleArrowf"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom"
                            Foreground="{StaticResource HIK}"
                            Kind="ReplyAll" MouseDown="MoveAllBack"   />-->
                <!--





                                </StackPanel>
                            </Border>
                        </Grid>

                        <StackPanel Grid.Column="2">
                            <Border
                                Height="auto"
                                BorderBrush="{StaticResource VISION}"
                                BorderThickness="1"
                                CornerRadius="5"
                                RenderTransformOrigin="0.5,0.5">



                                <StackPanel
                                    Height="336"
                                    Margin="0,0,0,0"
                                    HorizontalAlignment="Stretch">
                -->
                <!--  This is the hidden helper Grid which does the resizing  -->
                <!--
                                    <Grid Visibility="Hidden">
                                        <Grid.ColumnDefinitions>

                                            <ColumnDefinition Width="0.65*" />
                                            <ColumnDefinition Width="1*" />
                                            <ColumnDefinition Width="1*" />
                                            <ColumnDefinition Width="1.1*" />
                                            <ColumnDefinition Width="1*" />
                                            <ColumnDefinition Width="1*" />
                                            <ColumnDefinition Width="1*" />
                                        </Grid.ColumnDefinitions>
                -->
                <!--  This is the hidden helper Field which is used to bind to, using the "Fill" column of the helper grid  -->
                <!--
                                        <Grid x:Name="rc0" Grid.Column="0" />
                                        <Grid x:Name="rc1" Grid.Column="1" />
                                        <Grid x:Name="rc2" Grid.Column="2" />
                                        <Grid x:Name="rc3" Grid.Column="3" />
                                        <Grid x:Name="rc4" Grid.Column="4" />
                                        <Grid x:Name="rc5" Grid.Column="5" />
                                        <Grid x:Name="rc6" Grid.Column="6" />
                                        <Grid x:Name="rc7" Grid.Column="7" />
                                    </Grid>

                                    <ListView
                                        x:Name="ShiftAssignlist"
                                        Height="336"
                                        MaxHeight="336"
                                        Margin="0,0,4,0"
                                        VerticalAlignment="Top"
                                        VerticalContentAlignment="Top"
                                        Background="#fafafa"
                                        FontSize="12"
                                        Foreground="Black"
                                        ItemsSource="{Binding Udown_Disp}"
                                        ScrollViewer.CanContentScroll="True"
                                        ScrollViewer.HorizontalScrollBarVisibility="Visible"
                                        ScrollViewer.VerticalScrollBarVisibility="Visible"
                                        SelectionMode="Multiple">
                                        <ListView.Resources>
                                            <Style TargetType="{x:Type ListViewItem}">
                                                <Setter Property="Height" Value="0" />

                                            </Style>
                                        </ListView.Resources>

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="{x:Type ListViewItem}">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource HIK}" />
                                                        <Setter Property="Focusable" Value="True" />

                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource HIK}" />
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>


                                        </ListView.ItemContainerStyle>

                                        <ListView.View>

                                            <GridView>
                                                <GridView.ColumnHeaderContainerStyle>
                                                    <Style TargetType="{x:Type GridViewColumnHeader}">
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                                                                    <Border
                                                                        Background="Transparent"
                                                                        BorderBrush="Black"
                                                                        BorderThickness="0,0,0,0">
                                                                        <TextBlock
                                                                            x:Name="ContentHeader"
                                                                            Width="{TemplateBinding Width}"
                                                                            Padding="15,5,5,5"
                                                                            Text="{TemplateBinding Content}"
                                                                            TextAlignment="Left" />
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Setter Property="OverridesDefaultStyle" Value="True" />
                                                        <Setter Property="Foreground" Value="{StaticResource FREE}" />
                                                        <Setter Property="FontFamily" Value="Champagne &amp; Limousines" />
                                                        <Setter Property="FontSize" Value="12" />
                                                        <Setter Property="FontWeight" Value="Bold" />
                                                        <Setter Property="Background" Value="AliceBlue" />
                                                        <Setter Property="BorderBrush" Value="BlanchedAlmond" />
                                                        <Setter Property="BorderThickness" Value="2" />

                                                    </Style>
                                                </GridView.ColumnHeaderContainerStyle>



                                                <GridViewColumn Width="{Binding ActualWidth, ElementName=rc1}" Header="Dev ID">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                                Padding="0"
                                                                HorizontalAlignment="Stretch"
                                                                VerticalAlignment="Stretch"><InlineUIContainer>
                                                                    <Label
                                                                        Padding="0"
                                                                        Content="{Binding EMachineNumber}"
                                                                        FontSize="12" />
                                                                </InlineUIContainer></TextBlock>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>

                                                <GridViewColumn Width="{Binding ActualWidth, ElementName=rc2}" Header="User Id">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                                Padding="0"
                                                                HorizontalAlignment="Stretch"
                                                                VerticalAlignment="Stretch"><InlineUIContainer>
                                                                    <Label
                                                                        Padding="0"
                                                                        Content="{Binding EnrollNumber}"
                                                                        FontSize="12" />
                                                                </InlineUIContainer></TextBlock>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>

                                                <GridViewColumn Width="{Binding ActualWidth, ElementName=rc3}" Header="User Name">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                                Padding="0"
                                                                HorizontalAlignment="Stretch"
                                                                VerticalAlignment="Stretch"><InlineUIContainer>
                                                                    <Label
                                                                        Padding="0"
                                                                        Content="{Binding EName}"
                                                                        FontSize="12" />
                                                                </InlineUIContainer></TextBlock>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>

                -->
                <!--<GridViewColumn Width="{Binding ActualWidth, ElementName=rc4}" Header="Name">
                                                        <GridViewColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <TextBlock
                                            Padding="0"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Stretch"><InlineUIContainer>
                                                                            <Label
                                                                                Padding="0"
                                                                                Content="{Binding EnrollNumber}"
                                                                                FontSize="12" />
                                                                        </InlineUIContainer></TextBlock>
                                                            </DataTemplate>
                                                        </GridViewColumn.CellTemplate>
                                                    </GridViewColumn>-->
                <!--

                                                <GridViewColumn
                                                    x:Name="shift_r"
                                                    Width="{Binding ActualWidth, ElementName=rc5}"
                                                    Header="Shift">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                                Padding="0"
                                                                HorizontalAlignment="Stretch"
                                                                VerticalAlignment="Stretch"><InlineUIContainer>
                                                                    <Label
                                                                        Padding="0"
                                                                        Content="{Binding ShiftId}"
                                                                        FontSize="12" />
                                                                </InlineUIContainer></TextBlock>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>

                                                <GridViewColumn Width="{Binding ActualWidth, ElementName=rc6}" Header="Privilige">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                                Padding="0"
                                                                HorizontalAlignment="Stretch"
                                                                VerticalAlignment="Stretch"><InlineUIContainer>
                                                                    <Label
                                                                        Padding="0"
                                                                        Content="{Binding Privilige}"
                                                                        FontSize="12" />
                                                                </InlineUIContainer></TextBlock>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>

                                                <GridViewColumn Width="{Binding ActualWidth, ElementName=rc7}" Header="Password">
                                                    <GridViewColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <TextBlock
                                                                Padding="0"
                                                                HorizontalAlignment="Stretch"
                                                                VerticalAlignment="Stretch"><InlineUIContainer>
                                                                    <Label
                                                                        Padding="0"
                                                                        Content="{Binding enPassword}"
                                                                        FontSize="12" />
                                                                </InlineUIContainer></TextBlock>
                                                        </DataTemplate>
                                                    </GridViewColumn.CellTemplate>
                                                </GridViewColumn>



                                            </GridView>
                                        </ListView.View>

                                    </ListView>
                                </StackPanel>

                            </Border>

                        </StackPanel>



                    </Grid>



                </Grid>-->
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        Grid.Column="0"
                        Margin="0,00,0,0"
                        HorizontalAlignment="Left">
                        <Label
                            Name="lbldevStatus"
                            Content="StatusWindow"
                            FontSize="20"
                            Foreground="#417AAB" />
                    </TextBlock>
                </Grid>

                <Label
                    Name="StatusWindow"
                    Width="275"
                    Height="55"
                    HorizontalAlignment="Left" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
